# Copy to .env.local and fill in. The same two go into Vercel's
# Environment Variables so the deployed site can reach the database.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon public key>
